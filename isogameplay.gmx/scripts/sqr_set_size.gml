///sqr_set_size(sqr_id, length, width, height);

with(argument0)
{
    length = argument1;
    width = argument2;
    height = argument3;
    //var soffx = width * TCELLSIZETOX;
    //var soffy = sprite_height - (width + length - 2) * TCELLSIZETOZ;
    //sprite_set_offset(sprite_index, soffx, soffy);
}
