///is_decor(obj);

if (object_is_ancestor(argument0.object_index, obj_sqr_parent) ||
        object_is_ancestor(argument0.object_index, obj_pick_up))
        {
            return false;
        }
return true;
